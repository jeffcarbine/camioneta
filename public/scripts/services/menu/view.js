angular
  .module('menu.view', [
    'menu',
  ])
  .factory('view', [
    '$http',
    'host',
    function($http, host) {
      var view = {
        all: function() {

        },
      };
      return update;
    }
  ]);
